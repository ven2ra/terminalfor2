# Copy this file to .env (which is gitignored) and fill in real values.
# Never commit real tokens.

# T-Инвестиции API token (Т-Инвестиции -> Настройки -> API), "только чтение"
# достаточно. Enables the real order-book depth panel; without it the app
# shows a clearly-labelled simulated order book instead.
TINVEST_TOKEN=
