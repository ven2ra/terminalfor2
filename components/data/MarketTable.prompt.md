The market ledger. Numbers are pre-formatted strings so alignment is exact; large figures use the mono face, prices use semibold core.

```jsx
<MarketTable rows={rows} />
```

Rows are separated by hairlines only — no zebra striping. Hover lifts the row background 4.5%.
