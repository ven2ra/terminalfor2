Hero numeric. The last three characters fade to `--n-400` so the significant digits read first — a signature of the brand's number treatment.

```jsx
<PriceValue value={109687.6} />
<PriceValue value="4,837.00" suffix="USD" />
```
