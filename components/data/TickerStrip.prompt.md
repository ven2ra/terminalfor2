The 34px price ribbon that spans the full app width directly under the topbar. Items are separated by 40px of space, never by dividers.

```jsx
<TickerStrip items={[{symbol:'sol',price:'$176.34'},{symbol:'ltc',price:'$0.2329'}]} />
```
