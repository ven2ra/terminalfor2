Titles every page section. Pair the `live` dot with a freshness eyebrow whenever the data below streams.

```jsx
<SectionHeader live eyebrow="Last update 2 min ago" size="lg" title="Live Crypto Updates"
  actions={<Button variant="ghost" trailingIcon="chevron-right">See all</Button>} />
```
