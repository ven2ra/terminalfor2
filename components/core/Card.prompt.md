Matte container for every grouped block. Cards are flat: 1px `--border-hairline`, no drop shadow; depth comes from surface value.

```jsx
<Card variant="panel" padding="var(--pad-panel)">…</Card>
<Card interactive glow="up">…</Card>
```
