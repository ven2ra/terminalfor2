Signed % pill — green up, red down, always tinted background, never solid fill. Pass a raw number; the component formats sign and two decimals.

```jsx
<DeltaChip value={1.09} showIcon />
<DeltaChip value={-2.01} size="sm" />
```
