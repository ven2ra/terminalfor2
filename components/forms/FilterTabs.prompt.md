Row of pill filters above a data table. The selected pill takes the violet tint + violet hairline; the rest stay inset neutral.

```jsx
<FilterTabs options={['All','Trends','Favorites','Top Gainers','Top Losers']} value={tab} onChange={setTab} />
```
