Trade-panel money row. Stack two of them (Spend / Receive) with a circular swap IconButton overlapping the seam.

```jsx
<AmountField label="Spend" value="90,020.9" currency={<SelectMenu options={['USDT']} value="USDT" leading={<CoinMark symbol="usdt" size={16} />} />} />
```
