Mutually exclusive mode switch — Buy / Sell / Swap in the exchange panel. 2–3 short options only; use FilterTabs for 4+.

```jsx
<SegmentedControl options={['Buy','Sell','Swap']} value={mode} onChange={setMode} />
```
